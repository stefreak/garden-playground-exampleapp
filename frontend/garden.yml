kind: Module
type: container
name: frontend
dockerfile: Dockerfile
services:
  - name: frontend
    ports:
      - name: http
        containerPort: 8080
    ingresses:
      - path: /hello-frontend
        port: http
        hostname: frontend-frontend
      - path: /call-backend
        port: http
        hostname: frontend-backend
    dependencies:
      - backend